type Book {
  id: ID!
  title: String!
  author: Author @relationship(type: "WROTE", direction: IN)
}

type Author {
  id: ID!
  name: String!
  dateOfBirth: Date
  books: [Book] @relationship(type: "WROTE", direction: OUT)
}
